# Region 12 — CaucasusMiddleEast
# RGI region code: 12

rgi_code: "12"

# RGI attribute file for this region.
# Full path is resolved as ${rgi_base_path}/NN_rgi60_RegionName.csv
# Set rgi_base_path in config.yaml.
# Format options: "csv" (RGI 6.0) or "shp" (RGI 7.0)
rgi_attr_path: ${rgi_base_path}/12_rgi60_CaucasusMiddleEast.csv
rgi_format: csv

# Hemisphere — set to null to infer automatically from CenLat (recommended).
# Override: "NH" or "SH" (use for tropical regions near the equator, r16)
hemisphere: NH
