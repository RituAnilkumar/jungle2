# Absolute residual nonconformity scores
# Works with any model — only the predicted mean is needed.
# Produces constant-width prediction intervals.

type: absolute_residuals
alpha: 0.1       # miscoverage level → 90% marginal coverage
